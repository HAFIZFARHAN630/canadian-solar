# Dependencies
node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# Testing
/coverage

# Next.js build output
/.next/
/out/

# Production build
/build

# Standalone build output (used for cPanel deployment, not source)
/standalone/

# Misc
.DS_Store
*.pem

# Debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Env files (NEVER commit secrets)
.env
.env.local
.env.*.local
.env.production
# Keep .env.example tracked (template only)
!.env.example

# Vercel
.vercel

# TypeScript
*.tsbuildinfo
next-env.d.ts
local-*

# IDE / AI tooling
.claude
.z-ai-config

# Logs
*.log
dev.log
dev.out.log
server.log

# Process ID files
*.pid

# SQLite databases (NEVER commit data files)
db/*.db
db/*.db-journal
db/*.sqlite
db/*.sqlite3

# Tool-results / debug artifacts
/tool-results/

# Upload / Download scratch directories (per-environment, not source)
/download/
/upload/

# Skills directory (internal tooling)
/skills/

# cPanel deployment artifacts
/.cpanel.yml
/cpanel-deploy/
/cpanel-deploy.zip
